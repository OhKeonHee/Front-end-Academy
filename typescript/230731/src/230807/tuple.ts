// 튜플타입으로 타입정의하기 -> 배열타입 > 튜플 (❗ 튜플은 배열타입 하위개념)
let array: number[] = [1, 2, 3, 4];
const tuple: [boolean, string] = [true, "the result is ok"]; 
// 배열의 타입이 복수인 경우, 대괄호앞에 타입을 쓰는것이 아닌, 대괄호 안에 타입을 정의
// 튜플: 배열 안에 속해있는 아이템의 타입이 복수인 경우, 타입정의

// 튜플은 언제 사용할까?
// 배열 / 프로미스 객체를 사용할 때, 결과값을 반환하는 타입